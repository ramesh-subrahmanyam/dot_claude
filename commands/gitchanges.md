which files have changed in this repository?  For each such file, Display the git add and git commit commands, one per file.  The commit messages should only be 1-2 sentences and should describe the nature of the change.

For example:

git add x.py
git commit -m "handled attempts to read from stale file handles"

git add y.py
git commit -m "supported user specification of spectral cut-off"
